# Build artifacts
*.exe
backtest_reports/
*.dll
*.so
*.dylib
*.msi
*.wasm
node_modules/
dist/
build/
.next/
out/
coverage/
*.egg-info/
__pycache__/
*.pyc
.venv/
venv/
.ruff_cache/
.pytest_cache/

# Environment and secrets
.env
.env.local
.env.*.local
infra/env/*.env
*.pem
*.key
secrets/
mcp.env
jwt_secret.txt
database_url.txt

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test/coverage
*.cover
.coverage
htmlcov/
.nyc_output/
junit.xml
test-results/

# Docker
.docker/

# Runtime logs and audit artifacts
logs/
*.log
audit/*.json

# Go binaries
realtime/bin/
windows-agent/bin/

# NestJS
control/dist/

# Data (historical CSVs — too large for git)
data/xauusd_historical/

# Keep project controls
!.codex/
!.agents/

# Backup files
*.bak

# Temporary/working files
error.txt
prompt.md
predictatrade-live-patched.html
summary-v1.md
PROJECT_MEMORY.txt
opencode.json
.cleanup_backup/
windows-agent/agent

# Code signing private key — never commit
windows-agent/certs/pat-code-sign.key
windows-agent/certs/pat-code-sign.pfx

# Go realtime build artifact (built inside Docker, never commit)
realtime/realtime-engine

# Local debug screenshots (not source)
Screenshot_*.png

# Runtime-generated live calibration retrains (regenerated hourly by the engine)
realtime/calibration/calibration_*_live.json

# Client broker statement (PII: contains account number) — keep local only
report.xlsx
*.tsbuildinfo
mail-relay/mail-relay
infra/env/ADMIN_CREDENTIALS.local.md

# Go realtime build artifact (built inside Docker, never commit)
realtime/live-terminal
