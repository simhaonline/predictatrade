# Gitleaks configuration for Predict-A-Trade XAUUSD
# https://github.com/gitleaks/gitleaks

[extend]
useDefault = true

# Allowlist: known development-only test fixtures and non-secret patterns
# These are intentionally excluded from secret scanning because they are
# development/local-test credentials that are NOT used in production.

[[allowlist]]
description = "Known development-only database password used in test fixtures"
regex = '''pat_local_dev_only'''

[[allowlist]]
description = "Test files with known insecure password fixtures for security validation tests"
paths = [
  '''realtime/internal/config/config_test\.go''',
  '''control/src/common/__tests__/security-validation\.spec\.ts''',
]

[[allowlist]]
description = "Infrastructure env example files (non-secret templates)"
paths = [
  '''infra/env/.*\.env\.example''',
  '''mcp\.env\.example''',
]

[[allowlist]]
description = "Database URL in scripts uses env var with empty fallback"
paths = [
  '''scripts/verify_math_parity\.py''',
  '''scripts/train_ml_model\.py''',
]
