# Rollback Runbook

Do not rewrite or delete historical financial rows. Disable the four commercial feature flags and route traffic to the prior plan/event behavior. If a forward correction is required, add a new migration; use backup/PITR for an operational database rollback. Migration 024 has no destructive down migration by design.

