{
    "notification_type": "telegram",
    "telegram_bot_token": "YOUR_BOT_TOKEN",
    "telegram_chat_id": "YOUR_CHAT_ID",
    "discord_webhook": "YOUR_DISCORD_WEBHOOK_URL",
    "email_smtp": "smtp.yourdomain.com",
    "email_port": 587,
    "email_user": "",
    "email_password": "",
    "email_to": "alerts@yourdomain.com",
    "email_from": "predictatrade@yourdomain.com",
    "_health_check_url_note": "health-check.ps1 ignores this field and probes the per-role port: client :9000, master :9001 (role passed via -Mode). Kept for backward compatibility only.",
    "health_check_url": "http://localhost:9000/health",
    "health_check_timeout_seconds": 5,
    "health_check_interval_minutes": 1,
    "show_popup_notifications": true,
    "show_toast_notifications": true
}