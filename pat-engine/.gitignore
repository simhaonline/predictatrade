# runtime signal file produced by the gateway / simulator
signals/PAT_signals.txt
*.txt

# local secrets (the sample is ENV_SAMPLE, which is committed)
infra/env/.env

# build artifacts
*.exe
pat-engine.bin
backtest
dist/

# downloaded real-market CSVs (regenerate via scripts/fetch_xauusd.py)
data/
